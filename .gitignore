__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# data & experiment outputs
# NOTE: keep these ANCHORED with a leading slash. A bare `data/` matches any
# directory named data at any depth — it silently excluded the whole
# src/lsteer/data/ package from git until 2026-07-30.
/data/
/outputs/
wandb/
runs/
*.zarr/
*.ckpt
*.pt
*.mp4
